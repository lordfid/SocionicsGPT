PERBAIKAN DEPLOY LAMBAT — SocionicsGPT

HASIL PEMERIKSAAN:
- .npmrc saat ini sudah benar.
- vercel.json saat ini secara struktur sudah benar.
- package-lock.json justru tidak ada.
- Tanpa package-lock.json, npm harus menghitung ulang versi dependency saat setiap deployment.
- File dalam paket ini menambahkan package-lock.json bersih dari registry publik dan mengubah instalasi menjadi npm ci.

LANGKAH:
1. Upload/replace tiga file ini di ROOT repository, sejajar dengan package.json:
   - package-lock.json
   - .npmrc
   - vercel.json
2. Jangan taruh di dalam src/.
3. Di Vercel pilih Redeploy tanpa build cache untuk percobaan pertama.
4. Deployment berikutnya dapat memakai cache normal.

PENGATURAN:
- Framework: Vite
- Root Directory: ./
- Build Command: npm run build
- Output Directory: dist

CATATAN:
package-lock.json ini berasal dari dependency tree proyek v2.0.1 yang sama, tetapi seluruh URL registry internal sudah diganti menjadi https://registry.npmjs.org/.
